name: 更新论文雷达

on:
  push:
    branches:
      - main
    paths:
      - "src/papers.py"
      - "update_papers.py"
      - "audit_paper_sources.py"
      - "config/paper_sources.csv"
      - "requirements.txt"
      - ".github/workflows/update-papers.yml"
  schedule:
    - cron: "20 1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  # ⚠️ 两个机器人工作流（价格 / 论文）**共用同一个组**：它们都往 main 提交，
  # 各用各的组时可以并行跑，于是互相把对方的 push 顶掉。
  # 排队执行既避免了 non-fast-forward，也避免两个进程同时写 out/。
  group: repo-write
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 检查论文来源并补全缺失元数据
        shell: bash
        run: |
          set -o pipefail
          : > out/papers_update.log
          update_succeeded=false

          for attempt in 1 2 3; do
            echo "=== 论文更新尝试 ${attempt}/3 ===" | tee -a out/papers_update.log
            if python -u update_papers.py --force --rebuild --max-enrich 500 2>&1 | tee -a out/papers_update.log; then
              update_succeeded=true
              break
            fi

            if [ "${attempt}" -lt 3 ]; then
              wait_seconds=$((attempt * 45))
              echo "本次更新失败，${wait_seconds} 秒后重试。" | tee -a out/papers_update.log
              sleep "${wait_seconds}"
            fi
          done

          if [ "${update_succeeded}" != "true" ]; then
            echo "论文更新连续失败 3 次，完整日志已上传到工作流产物。" | tee -a out/papers_update.log
            exit 1
          fi

          python audit_paper_sources.py --offline --json out/paper_sources_audit.json

      - name: 提交论文产物
        run: |
          set -euo pipefail
          git add out/papers.json out/papers.csv out/papers_state.json out/paper_sources_audit.json

          if git diff --cached --quiet; then
            echo "论文数据无变化，跳过提交。"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: 更新论文雷达 ($(date -u +%Y-%m-%d))"
          git pull --rebase --autostash origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      - name: 上传论文数据
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ai-paper-radar
          path: |
            out/papers.json
            out/papers.csv
            out/papers_state.json
            out/paper_sources_audit.json
            out/papers_update.log
          retention-days: 14

